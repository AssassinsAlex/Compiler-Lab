int check(int a, int b, int c, int d, int e){
    write(a);
    write(b);
    if(a < 100)
        return 1;
    write(c);
    write(d);
    write(e);
    return 0;
}
int main() {
    int i = 11;
    check(i+31, i*3+1, i, i-3, i/3+22);
    check(i*11, i, i+i+i+i, i*i, i+1);
    return 0;
}